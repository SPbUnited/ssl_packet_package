syntax = "proto2";

package robot.telemetry;

message RobotTelemetry {
    required bool ball_in = 1;
    required bool ball_deep = 2;
    required float kicker_voltage = 3;
    optional float battery_voltage = 4;
}